{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default Skillage desktop permissions. Local scanning uses narrow Rust commands instead of broad filesystem plugin access.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
